# Prettier formats Markdown here and nothing else — the rest belongs to Biome. Out of its reach
# stays what a human narrowed by hand and what a generator writes: a `--write` pass would fight
# both and give a worthless diff.
node_modules/
download/
.scribe-devtools/
.ws/

# Hand-wrapped prose with tables narrowed to a readable width. Prettier pads table cells to the
# widest row, which turns a table a human shaped into one very long line per row.
README.md
AGENTS.md

# Generated by `pnpm run code-index`.
CODE-INDEX.md
