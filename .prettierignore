# Prettier formats the code and the configuration. Out of its reach stays what a human maintains
# by hand in tables narrowed to a readable width, and what a generator writes (a `--write` pass
# would fight the generator and give a worthless diff).
node_modules/
coverage/
package-lock.json
.scribe-devtools/

# Hand-maintained prose with narrowed tables.
README.md
AGENTS.md
docs/DESIGN.md
docs/PLAN.md
docs/ACCEPTANCE.md
docs/handoff/

# Written by `nx-angular-inspector` (twin of the .gitignore entry).
.ws/

# Dropped by the @playwright/mcp server (during `npm run bench`, or from a hand-copied `mcp.json`).
.playwright-mcp/

# Generated.
CODE-INDEX.md
docs/STEPS.md
bench/RAPORT.md
bench/WYNIKI.md
bench/BUDGET.md
bench/out/
download/

# Pages under test and a committed app build — not this repository's code style.
**/fixtures/**
bench/app/

# Reconnaissance reports: a published artifact copied in verbatim, and its index.
docs/research/
